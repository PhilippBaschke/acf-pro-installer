language: php

# Support the same PHP versions as Bedrock
php:
  - 7.2
  - 7.3

install:
  - composer install --no-interaction

script:
  - sonar-scanner -X

# No need for email notifications
notifications:
  email: false

addons:
  sonarcloud:
    organization: pivvenit